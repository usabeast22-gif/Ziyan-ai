"""
Photo-to-video adapter.

Connect an image-to-video model here. Keep the function signature stable so
the Streamlit UI does not need to change when a model is connected.
"""

def generate_from_photo(image_path, prompt, seconds=5):
    raise NotImplementedError(
        "Connect an image-to-video model/API in this adapter."
    )
